import DS from 'ember-data';

export default DS.Model.extend({
  /* Need to find what the serializer looks like for this built-in Django
  model. Currently user fields are in UserProfile serializer */
});
